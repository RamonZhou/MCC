void printi(int v) {
    printf(": %d\n", v);
}

int gl = 2;

int main() {
    int array(12) array(3) arr;
    int a = 5 + 5;
    int b = a + 2;
    double f = 0.1;
    gl = 5;
    printf("--Hello world! %.1lf!\n" + 2, a + 1 + b + f);
    for (int i = 0; i <= 10.0; i = i + 1) {
        arr[1][i] = i;
        if (i == 5) {
            printi(arr[1][i] * gl);
        } else {
            printf("! i = %d\n", i);
        }
    }
    return 0;
}